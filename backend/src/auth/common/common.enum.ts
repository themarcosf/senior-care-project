export enum Auth {
  PASSPORT_STRATEGY = "local",
  JWT_AUTHGUARD = "jwt",
  IS_PUBLIC_KEY = "isPublic",
}

export enum Api {
  ADDR = "auth",
  SIGNUP = "signup",
  SIGNIN = "signin",
  SIGNOUT = "signout",
  PROFILE = "profile",
}
