export enum Api {
  ADDR = "api/v1/med-record",
  PATH = "./uploads/medTests/",
  PATIENT = "patient",
}

export enum QueryField {
  ID = "id",
  FULL_NAME = "fullName",
  TOGGLE_STATUS = "status",
}
